import LoginCard from '../components/module/LoginPage/LoginCard'

type Props = {}

const LoginPage = (props: Props) => {
  return (
    <div>
        <LoginCard />
    </div>
  )
}

export default LoginPage